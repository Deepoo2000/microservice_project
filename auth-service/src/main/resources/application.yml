spring:
  application:
    name: auth-service
  config:
    import: "optional:configserver:"
  cloud:
    config:
      uri: http://localhost:8071
    vault:
      host: vault
      port: 8243
      scheme: http
      authentication: token
      token: root